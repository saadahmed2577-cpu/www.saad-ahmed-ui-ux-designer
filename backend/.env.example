# Server Port & Environment
PORT=5000
NODE_ENV=development

# Allowed Frontend URL (CORS)
CLIENT_URL=http://localhost:3000

# MongoDB Atlas Connection URI
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/portfolio_db?retryWrites=true&w=majority

# JWT Secret & Expiry
JWT_SECRET=super_secret_jwt_key_please_change_in_production_2026
JWT_EXPIRE=7d

# Initial Admin Credentials (for seed-admin script)
ADMIN_USERNAME=saad_admin
ADMIN_EMAIL=saadahmed3803@gmail.com
ADMIN_PASSWORD=UI/UXSAQ

# SMTP Configuration (for Nodemailer Email OTP - Works with Gmail, Mailtrap, Brevo, SendGrid, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=saadahmed3803@gmail.com
SMTP_PASS=your_smtp_password_or_app_password
SMTP_FROM="Saad Ahmed Portfolio" <saadahmed3803@gmail.com>

# Cloudinary Credentials for Image Upload (Optional — direct image URLs also supported)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
